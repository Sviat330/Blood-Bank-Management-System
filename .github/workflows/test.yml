name: Test .env
on:
  workflow_dispatch:
jobs:
  test:
    enviorment: staging
    runs-on: ubuntu-latest
    steps: 
        - uses: actions/checkout@v3
        - name: Set ENV
          run: ./env-test.sh
          env:
            DB_USER: ${{ env.DB_USER }}
        - name: test ENV
          run: echo $DB_USER
